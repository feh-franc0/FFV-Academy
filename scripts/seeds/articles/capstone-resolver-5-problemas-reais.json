{
  "slug": "capstone-resolver-5-problemas-reais",
  "title": null,
  "blocks": [
    {
      "id": "3c892d30-0839-49f2-b134-b035a677da84",
      "type": "section",
      "position": 0,
      "data": {
        "title": "Problema 1 — Dedup de 10M eventos com Bloom"
      },
      "children": [
        {
          "id": "f8e06588-bc2a-4fea-88e8-e3252fc54599",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "typescript",
            "code": "import { BloomFilter } from 'bloom-filters';\n\nconst seen = new BloomFilter(10_000_000, 0.001);  // 1MB, 0.1% FP\n\nasync function processEvent(event: Event) {\n  if (!seen.has(event.id)) {\n    seen.add(event.id);\n    return await persistNew(event);\n  }\n  // Possível duplicata (ou falso positivo) — confirma no DB\n  const exists = await db.events.findUnique({ where: { id: event.id } });\n  if (exists) return;  // dup real\n  seen.add(event.id);\n  await persistNew(event);\n}"
          }
        }
      ]
    },
    {
      "id": "4d936f88-9115-478b-b924-ceae804b38d4",
      "type": "section",
      "position": 1,
      "data": {
        "title": "Problema 2 — Top 10 queries lentas com min-heap"
      },
      "children": [
        {
          "id": "6a52f62d-485b-41fd-acd7-6f1daceef58c",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "typescript",
            "code": "import TinyQueue from 'tinyqueue';\n\ninterface QueryStat { sql: string; duration: number; }\n\nfunction topSlow(stream: QueryStat[], k = 10): QueryStat[] {\n  const heap = new TinyQueue<QueryStat>([], (a, b) => a.duration - b.duration);\n  for (const q of stream) {\n    if (heap.length < k) heap.push(q);\n    else if (q.duration > heap.peek()!.duration) {\n      heap.pop();\n      heap.push(q);\n    }\n  }\n  return [...heap].sort((a, b) => b.duration - a.duration);\n}"
          }
        }
      ]
    },
    {
      "id": "ea710027-42a0-4604-97ab-895b95d05a50",
      "type": "section",
      "position": 2,
      "data": {
        "title": "Problema 3 — Ciclo em deps de pacote"
      },
      "children": [
        {
          "id": "4bdedf72-9fc4-4457-88ea-64da0632cdd2",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "typescript",
            "code": "type Deps = Map<string, string[]>;\n\nfunction findCycle(deps: Deps): string[] | null {\n  const WHITE = 0, GRAY = 1, BLACK = 2;\n  const color = new Map<string, number>();\n  const parent = new Map<string, string>();\n\n  function dfs(u: string): string[] | null {\n    color.set(u, GRAY);\n    for (const v of deps.get(u) ?? []) {\n      if (color.get(v) === GRAY) {\n        // Reconstrói ciclo\n        const cycle = [v, u];\n        let cur = u;\n        while (parent.get(cur) !== v) {\n          cur = parent.get(cur)!;\n          cycle.push(cur);\n        }\n        return cycle.reverse();\n      }\n      if (color.get(v) === WHITE || !color.has(v)) {\n        parent.set(v, u);\n        const c = dfs(v);\n        if (c) return c;\n      }\n    }\n    color.set(u, BLACK);\n    return null;\n  }\n\n  for (const u of deps.keys()) {\n    if (!color.has(u)) {\n      const cycle = dfs(u);\n      if (cycle) return cycle;\n    }\n  }\n  return null;\n}"
          }
        }
      ]
    },
    {
      "id": "ef11ec71-7351-4f8c-907f-460d09027363",
      "type": "section",
      "position": 3,
      "data": {
        "title": "Problema 4 — Cursor pagination com tiebreaker"
      },
      "children": [
        {
          "id": "47094d2e-dcb9-4e76-84d5-fbdc78380f21",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "typescript",
            "code": "// SQL (PG)\n// SELECT * FROM posts\n// WHERE (created_at, id) < ($lastAt, $lastId)\n// ORDER BY created_at DESC, id DESC\n// LIMIT 20;\n\nfunction encodeCursor(row: { createdAt: string; id: string }): string {\n  return Buffer.from(JSON.stringify(row)).toString('base64url');\n}\n\nfunction decodeCursor(s: string): { createdAt: string; id: string } {\n  return JSON.parse(Buffer.from(s, 'base64url').toString());\n}"
          }
        }
      ]
    },
    {
      "id": "60f36575-fb53-467c-8436-b065eafed30a",
      "type": "section",
      "position": 4,
      "data": {
        "title": "Problema 5 — Rate limit sliding window"
      },
      "children": [
        {
          "id": "c39a78b8-1f39-491d-90ef-27eecda041ae",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "lua",
            "code": "-- Redis Lua — sliding window counter\n-- KEYS[1] = key, ARGV[1] = window_s, ARGV[2] = max, ARGV[3] = now_ms\n\nlocal window_ms = tonumber(ARGV[1]) * 1000\nlocal max = tonumber(ARGV[2])\nlocal now = tonumber(ARGV[3])\n\n-- Remove entries antigos\nredis.call('ZREMRANGEBYSCORE', KEYS[1], 0, now - window_ms)\n\nlocal count = redis.call('ZCARD', KEYS[1])\nif count >= max then\n  return {0, count}  -- rate limited\nend\n\nredis.call('ZADD', KEYS[1], now, now .. '-' .. math.random())\nredis.call('EXPIRE', KEYS[1], ARGV[1])\nreturn {1, count + 1}"
          }
        },
        {
          "id": "35f451d9-a3a3-4842-9585-2d1cf0d447d9",
          "type": "callout",
          "position": 1,
          "data": {
            "variant": "success",
            "title": "",
            "content": "Esses 5 exemplos cobrem Bloom, heap, DFS/cycle, sorted compound cursor e Redis ZSET+Lua. Todos aparecem diariamente em produção de verdade — nada de permutação de strings."
          }
        }
      ]
    }
  ]
}