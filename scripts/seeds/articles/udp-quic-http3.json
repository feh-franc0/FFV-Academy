{
  "slug": "udp-quic-http3",
  "title": null,
  "blocks": [
    {
      "id": "q578-0",
      "type": "paragraph",
      "position": 0,
      "data": {
        "content": [
          {
            "text": " UDP é o protocolo sem garantias — sem conexão, sem ordem, sem retransmissão. Isso que o torna ideal para tempo real. QUIC reimplementa as partes boas do TCP em cima do UDP, sem carregar suas limitações de décadas. HTTP/3 é a versão da web sobre QUIC. "
          }
        ]
      }
    },
    {
      "id": "q578-1",
      "type": "section",
      "position": 1,
      "data": {
        "title": "UDP: o protocolo sem garantias"
      },
      "children": [
        {
          "id": "q578-2",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "text",
            "code": "import socket\nimport time\n\n# UDP: User Datagram Protocol\n# Header mínimo: src_port(2) + dst_port(2) + length(2) + checksum(2) = 8 bytes\n# TCP header: 20+ bytes. UDP: 8 bytes.\n# Sem: conexão, confirmação, ordering, controle de fluxo, congestionamento\n\n# Servidor UDP simples:\ndef udp_server(host: str = \"0.0.0.0\", port: int = 5005):\n    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\n    sock.bind((host, port))\n    print(f\"UDP server ouvindo em {host}:{port}\")\n    while True:\n        data, addr = sock.recvfrom(65535)  # máximo de um datagrama UDP\n        print(f\"De {addr}: {data.decode()}\")\n        sock.sendto(b\"ok\", addr)\n\n# Cliente UDP simples:\ndef udp_client(host: str, port: int, message: str):\n    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\n    sock.settimeout(1.0)\n    sock.sendto(message.encode(), (host, port))\n    try:\n        resp, _ = sock.recvfrom(1024)\n        return resp.decode()\n    except socket.timeout:\n        return None  # datagrama perdido — sem retransmissão automática\n\n# DNS usa UDP (porta 53):\n# Query simples (< 512 bytes) → UDP\n# Respostas grandes (DNSSEC) → TCP (truncated flag indica necessidade)\n# dig usa UDP por padrão; dig +tcp força TCP\n\n# Datagramas UDP não garantem:\n# 1. Entrega (pode ser descartado por router/firewall)\n# 2. Ordem (seg 1 pode chegar depois de seg 2)\n# 3. Integridade (checksum optional no IPv4 — pode ser zerado)\n# 4. Unicidade (pode duplicar em retransmissões de L2)\n\n# Casos onde UDP ganha:\n# - RTT único (DNS, DHCP) — mais rápido que TCP handshake\n# - Dados em tempo real (voz, vídeo) — dado velho inútil\n# - Broadcast/multicast (TCP é ponto-a-ponto apenas)\n# - Jogos online (posição, inputs — dado velho descartável)"
          }
        }
      ]
    },
    {
      "id": "q578-3",
      "type": "section",
      "position": 2,
      "data": {
        "title": "QUIC: TCP reimaginado sobre UDP"
      },
      "children": [
        {
          "id": "q578-4",
          "type": "comparison_table",
          "position": 0,
          "data": {
            "columns": [
              "Característica",
              "TCP+TLS",
              "QUIC (UDP)"
            ],
            "rows": [
              [
                "Handshake nova conexão",
                "3 RTTs (TCP+TLS 1.3)",
                "1 RTT"
              ],
              [
                "Handshake reconexão",
                "1 RTT (TLS session)",
                "0 RTT"
              ],
              [
                "Múltiplos streams",
                "Não (1 stream por conexão)",
                "Sim (independentes)"
              ],
              [
                "HoL blocking",
                "Sim (transporte)",
                "Não (por stream)"
              ],
              [
                "Mobilidade de IP",
                "Não (quebra conexão)",
                "Sim (Connection ID)"
              ],
              [
                "Header encryption",
                "Não",
                "Sim (IP+porta apenas visíveis)"
              ],
              [
                "User-space stack",
                "Não (kernel)",
                "Sim (flexível, iterável)"
              ]
            ]
          }
        },
        {
          "id": "q578-5",
          "type": "code_block",
          "position": 1,
          "data": {
            "language": "text",
            "code": "# QUIC (RFC 9000): protocolo de transporte multiplexado e seguro\n# Implementado em userspace — facilita evolução sem mudança de kernel\n\n# Estrutura de um pacote QUIC:\n# [UDP header 8B] [QUIC header: Connection ID + packet num + version] [frames]\n# Frames QUIC dentro do payload (criptografados):\n# - STREAM: dados de um stream específico\n# - ACK: confirmação de pacotes recebidos\n# - CRYPTO: handshake TLS 1.3\n# - RESET_STREAM: cancela um stream individual\n# - CONNECTION_CLOSE: fecha a conexão\n\n# Streams independentes: a chave do HoL blocking fix\n# Stream 1: HTML\n# Stream 2: CSS      ← se perda de pacote QUIC aqui\n# Stream 3: JS       ← este stream NÃO é bloqueado\n# Stream 4: imagem   ← este stream NÃO é bloqueado\n# Diferente do TCP onde perda bloqueia TODOS os dados subsequentes\n\n# Connection ID: permite mobilidade de IP\n# TCP identifica conexão por (src_ip, src_port, dst_ip, dst_port)\n# Mudar de Wi-Fi para 4G → src_ip muda → conexão TCP quebra\n# QUIC usa Connection ID opaco (64 bits) — não muda com IP\n# Celular muda de rede → QUIC continua sem re-handshake\n\n# 0-RTT connection resumption:\n# 1ª conexão: 1 RTT handshake. Servidor envia \"session ticket\" criptografado.\n# 2ª conexão: cliente envia dados da aplicação COM o primeiro pacote QUIC.\n# Servidor decripta session ticket → recupera chaves de sessão → decripta dados.\n# Total: 0 RTTs adicionais de handshake (limitado a dados idempotentes)\n\n# Implementações QUIC em Python:\n# aioquic: https://github.com/aiortc/aioquic\n# Exemplo de cliente HTTP/3:\n# python -m aioquic.examples.http3_client https://quic.nginx.org/\n\n# Verificar se site suporta QUIC/HTTP3:\n# curl --http3 https://cloudflare.com -I\n# chrome://net-internals/#quic (Chrome DevTools)"
          }
        },
        {
          "id": "udp-quic-v1",
          "type": "layer_stack",
          "data": {
            "title": "Onde o QUIC entra na pilha — e por que ele muda tudo",
            "layers": [
              {
                "label": "HTTP/3",
                "content": "A semântica de requisição e resposta",
                "note": "Igual em espírito ao HTTP/2, só que rodando sobre QUIC",
                "tone": "default"
              },
              {
                "label": "QUIC",
                "content": "Controle de congestionamento + cifra + multiplexing, tudo aqui",
                "note": "Faz o trabalho que TCP+TLS faziam separados — por isso conecta mais rápido",
                "tone": "writable"
              },
              {
                "label": "UDP",
                "content": "Transporte sem garantia nenhuma, só entrega datagrama",
                "note": "QUIC é construído EM CIMA do UDP porque o SO já sabe rotear UDP — reescrever o transporte sem mudar kernel",
                "tone": "base"
              },
              {
                "label": "IP",
                "content": "Roteamento entre redes",
                "note": "A camada que não muda, seja TCP ou UDP por cima",
                "tone": "base"
              }
            ],
            "separatorLabel": "roda sobre",
            "variant": "default"
          },
          "position": 2
        }
      ]
    },
    {
      "id": "q578-15",
      "type": "quiz",
      "position": 3,
      "data": {
        "question": "Qual problema do transporte confiável tradicional o protocolo mais recente resolve ao rodar sobre datagramas?",
        "options": [
          "O limite de conexões simultâneas por cliente",
          "O bloqueio de cabeça de fila no transporte: com vários fluxos numa conexão, a perda de um pacote parava todos — agora só o fluxo afetado espera",
          "O custo de criptografia",
          "A necessidade de resolver nomes antes de conectar"
        ],
        "correctIndex": 1,
        "explanation": "Multiplexar vários fluxos sobre um transporte que entrega em ordem estrita faz um pacote perdido travar tudo, mesmo o que já chegou. Ao mover a confiabilidade para cima do datagrama, cada fluxo passa a se recuperar sozinho — ganho grande em rede com perda. Criptografia continua presente e integrada. Resolução de nome é etapa anterior. E limite de conexões é outro assunto."
      }
    },
    {
      "id": "q578-6",
      "type": "section",
      "position": 4,
      "data": {
        "title": "UDP para tempo real: RTP e WebRTC"
      },
      "children": [
        {
          "id": "q578-7",
          "type": "code_block",
          "position": 0,
          "data": {
            "language": "text",
            "code": "# RTP (Real-time Transport Protocol, RFC 3550):\n# Roda sobre UDP. Adiciona apenas o que importa para mídia:\n# - Sequence number (16 bits): detectar perda e reordenação\n# - Timestamp: sincronização de playback (não wall clock)\n# - SSRC: identifica a fonte de mídia\n# NÃO adiciona: retransmissão, controle de congestionamento\n# Controle de qualidade: RTCP (companion) reporta jitter, perda, RTT\n\nimport socket\nimport struct\nimport time\n\n# Header RTP (12 bytes):\n# V(2) P(1) X(1) CC(4) M(1) PT(7) | Sequence(16) | Timestamp(32) | SSRC(32)\ndef build_rtp_header(seq: int, timestamp: int, ssrc: int, payload_type: int = 96) -> bytes:\n    \"\"\"Constrói header RTP mínimo.\"\"\"\n    version = 0b10  # RTP version 2\n    first_byte = (version << 6)  # sem padding, sem extensão, CC=0\n    second_byte = payload_type & 0x7F  # PT sem marker\n    return struct.pack(\"!BBHII\", first_byte, second_byte, seq, timestamp, ssrc)\n\n# WebRTC: P2P real-time usando DTLS-SRTP sobre UDP\n# DTLS: TLS adaptado para datagramas (handles packet loss/reorder)\n# SRTP: RTP com criptografia (DTLS negocia chaves)\n# ICE: encontra o melhor caminho (direct UDP, STUN, TURN)\n\n# ICE (Interactive Connectivity Establishment):\n# 1. Gather candidates: endereço local, STUN (NAT reflexive), TURN (relay)\n# 2. Connectivity check: testa cada par de candidatos\n# 3. Nominate: escolhe o melhor caminho (preferência: direto > STUN > TURN)\n\n# STUN (Session Traversal Utilities for NAT):\n# Pergunta para servidor público: \"qual é meu IP:porta externa?\"\n# Servidor responde com o IP:porta que o NAT mapeou\n\n# TURN (Traversal Using Relays around NAT):\n# Relay para casos onde conexão direta falha (NAT simétrico)\n# Custo: tráfego passa pelo servidor relay\n\n# Jitter buffer: compensa variação de latência (jitter)\n# Atrasa reprodução em X ms para ter buffer de frames\n# Trade-off: menor atraso vs maior conforto (sem glitches)\n# WebRTC adapta o jitter buffer dinamicamente"
          }
        }
      ]
    },
    {
      "id": "q578-8",
      "type": "callout",
      "position": 5,
      "data": {
        "variant": "success",
        "title": "",
        "content": "Modelo mental: UDP = enviar e esquecer (sem garantias, mas 8 bytes de overhead e zero latência de handshake). Ideal quando dado velho é inútil (vídeo ao vivo, jogos, DNS). QUIC = TCP reimaginado sobre UDP: 0-1 RTT de handshake, streams independentes (sem HoL blocking), mobilidade de IP (Connection ID), header criptografado, implementado em userspace. HTTP/3 roda sobre QUIC — já usado por 25%+ dos sites do mundo (Cloudflare, Google, Meta)."
      }
    },
    {
      "id": "q578-9",
      "type": "callout",
      "position": 6,
      "data": {
        "variant": "info",
        "title": "",
        "content": "Próximo: HTTP/1.1, /2, /3 — como cada versão resolveu limitações da anterior e o que muda na prática."
      }
    },
    {
      "type": "section",
      "data": {
        "title": "Perguntas frequentes"
      },
      "children": [
        {
          "type": "qa_item",
          "data": {
            "question": "UDP é TCP sem garantias?",
            "answer": "Não: é um protocolo diferente, sem conexão, sem ordem e sem controle de congestionamento — e é justamente essa simplicidade que permite construir por cima algo com garantias diferentes. Tratá-lo como versão degradada do TCP é o que impede entender por que protocolos modernos o escolhem."
          },
          "id": "q578-11",
          "position": 0
        },
        {
          "type": "qa_item",
          "data": {
            "question": "Por que abandonar o TCP como base do protocolo web?",
            "answer": "Por dois problemas estruturais: o bloqueio de cabeça de fila entre fluxos multiplexados e a impossibilidade de evoluir o protocolo, que está implementado no núcleo dos sistemas. Construir sobre UDP em espaço de usuário resolve os dois — e é a razão do protocolo mais recente."
          },
          "id": "q578-12",
          "position": 1
        },
        {
          "type": "qa_item",
          "data": {
            "question": "HTTP/3 é sempre mais rápido?",
            "answer": "Não. Ele ganha em rede com perda e em conexão nova, e empata ou perde em rede boa com conexão já estabelecida. E há custo de CPU maior, por processar em espaço de usuário. O ganho aparece mais em rede móvel, que é onde a maior parte do tráfego está."
          },
          "id": "q578-13",
          "position": 2
        }
      ],
      "id": "q578-10",
      "position": 7
    },
    {
      "id": "q578-14",
      "type": "section",
      "position": 8,
      "data": {
        "title": "Fixando"
      },
      "children": [
        {
          "id": "q578-16",
          "type": "quiz",
          "position": 0,
          "data": {
            "question": "Quando escolher um transporte sem garantias é a decisão correta?",
            "options": [
              "Quando dado atrasado perde o valor — voz e vídeo ao vivo, telemetria de alta frequência: retransmitir um trecho antigo atrapalha mais que descartá-lo",
              "Quando a rede é confiável o suficiente para dispensar confirmações",
              "Quando não é necessário criptografar",
              "Quando o volume de dados é pequeno"
            ],
            "correctIndex": 0,
            "explanation": "O critério é a utilidade do dado no tempo: num áudio ao vivo, o pedaço perdido já passou, e insistir nele atrasa o que vem depois — descartar e seguir produz melhor experiência. Nenhuma rede é confiável a ponto de dispensar controle onde ele importa. Volume não decide. E criptografia é ortogonal ao transporte escolhido."
          }
        },
        {
          "id": "q578-17",
          "type": "quiz",
          "position": 1,
          "data": {
            "question": "Uma conexão sobre datagramas sobrevive à troca de rede — do dado móvel para wi-fi — sem reconectar. Por quê?",
            "options": [
              "Porque datagramas não têm estado",
              "Porque o servidor mantém a sessão aberta por tempo indeterminado",
              "Porque a conexão é identificada por um identificador próprio, não pela combinação de endereços e portas — mudar de rede não a invalida",
              "Porque o cliente mantém as duas interfaces ativas simultaneamente"
            ],
            "correctIndex": 2,
            "explanation": "Amarrar a conexão a um identificador independente do endereço é o que permite migrar de rede sem refazer handshake — na prática, a chamada não cai ao sair de casa. Datagramas realmente não têm estado, mas quem mantém o estado aqui é o protocolo acima. Tempo de sessão no servidor não resolveria a mudança de endereço. E manter duas interfaces ativas é outra técnica, com outro custo."
          }
        }
      ]
    }
  ]
}
