{
  "name": "@ffv/mcp-academy",
  "version": "0.2.0",
  "private": true,
  "description": "MCP server para a FFV Academy — expõe gestão de currículo (artigos) ao Claude.",
  "type": "module",
  "bin": {
    "ffv-mcp-academy": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^4.1.5",
    "typescript": "^5.7.2",
    "vitest": "^4.1.5"
  }
}
