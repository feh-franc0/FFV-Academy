name: ffv-prod

services:
  # ─── Nginx: proxy reverso + SSL + load balancer ─────────────────────────────
  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /opt/ffv/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /opt/ffv/nginx/conf.d:/etc/nginx/conf.d:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - certbot_webroot:/var/www/certbot:ro
    networks:
      - proxy
    depends_on:
      api:
        condition: service_healthy

  # ─── Go API (2 réplicas via --scale api=2 no deploy.sh) ─────────────────────
  # O Docker DNS resolve "api" para todas as réplicas saudáveis.
  # O Nginx upstream detecta falhas via max_fails e exclui a réplica doente.
  # Enquanto Docker reinicia a réplica com problema, 100% do tráfego vai para a outra.
  api:
    image: ghcr.io/feh-franc0/ffv-api:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: /opt/ffv/.env
    expose:
      - "8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/api", "--healthcheck"]
      interval: 15s
      timeout: 5s
      start_period: 20s
      retries: 3
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 60s
      resources:
        limits:
          memory: 512m
          cpus: "0.8"
        reservations:
          memory: 128m
          cpus: "0.1"
    networks:
      - proxy
      - data

  # ─── Frontend Next.js (SSR standalone) ─────────────────────────────────────
  frontend:
    image: ghcr.io/feh-franc0/ffv-frontend:${FRONTEND_TAG:-latest}
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_BASE_URL: https://api.fernandofrancovalle.com
    expose:
      - "3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: "0.8"
    networks:
      - proxy

  # ─── PostgreSQL ─────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ffv
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ffv_prod
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ffv -d ffv_prod"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: "0.8"
        reservations:
          memory: 256m
          cpus: "0.1"
    networks:
      - data

  # ─── Redis ──────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --appendonly yes
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 320m
          cpus: "0.3"
        reservations:
          memory: 64m
          cpus: "0.05"
    networks:
      - data

volumes:
  postgres_data:
  redis_data:
  certbot_webroot:

networks:
  proxy:
  data:
    internal: true
