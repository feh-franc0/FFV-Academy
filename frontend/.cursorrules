# Cursor Project Rules
# Generated by AI Codebase Toolkit.
<!-- aitk-manifest: ada9b9a678ea5464 -->

You are working on a typescript (strict TypeScript) project using next, react.

## Conventions
- File naming: `mixed` (91% consistent)
- Import style: mixed
- Test framework: vitest

## Structure
- Source root: `src`
- Top-level folders: `e2e`, `hostinger`, `public`, `scripts`, `src`, `test-results`
- Tests folder present

## Commands
- `npm run build` — `next build`
- `npm run test` — `vitest run`
- `npm run lint` — `eslint src/ --ext .ts,.tsx`
- `npm run dev` — `next dev`
- `npm run start` — `next start`

## Rules
1. Match existing file naming (mixed).
2. Prefer editing existing files over creating new ones.
3. Add tests next to the implementation file or under the existing test folder.
4. Do not introduce new dependencies without strong justification.
5. Keep changes minimal and focused on the asked task.
