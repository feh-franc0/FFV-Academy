# Playwright
playwright-report/
test-results/
/playwright/.cache/

# Env — segredos locais (templates .example SÃO commitados)
.env.local
.env
.env.*.local

# Backups gerados pela migração de schema do question-bank (v1 → v2)
data/question-bank/*.v1-backup.json
