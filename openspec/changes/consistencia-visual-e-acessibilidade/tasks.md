## 1. Contraste dos controles (P0 primeiro)

- [x] 1.1 Trocar `'white'`/`'#fff'` por `var(--primary-foreground)` em todo botão de fundo acentuado (~35 pontos), começando por "Fazer login" e "Comentar" — re-medido em 11/ago: 40 ocorrências (não 43; "Comentar"/`CommentSection.tsx` já estava corrigido por `estados-de-ui-resilientes`). 31 corrigidas; 4 ficam de fora por medirem contraste OK (`#0a66c2` LinkedIn, 5,69:1 — `Certificate.tsx`, `DevProfileClient.tsx:726`, `DevCardClient.tsx:254`, `TrailCompletionModal.tsx`); 4 ficam de fora por serem overlay decorativo sobre foto, não controle de acento (`NewsCard.tsx`, 4 pontos); 1 (`NextModuleCard.tsx`) removido em 2.2 em vez de corrigido. `trail.color`/`cert.color` (cor de DADO, não var de tema) não podem usar `var(--primary-foreground)` — arbitrárias e fixas por tema, algumas escuras (ex. `#336791` falha com texto escuro). Novo `src/lib/readable-text.ts` (`readableTextColor()`, testado contra toda cor real de trilha/hub/cert em `readable-text.test.ts`) resolve caso a caso. Achado durante a varredura desta tarefa: o PRÓPRIO `FfvButton` (o botão que 2.1 elege como único) tinha `color:'#fff'` fixo na variante `primary` — passava no tema claro e falhava 2,53:1/1,95:1 no ESCURO (o padrão do site); corrigido para `var(--primary-foreground)`, testado em `FfvButton.test.tsx`. Mais 2 instâncias do mesmo padrão (`trail.color` + texto fixo) achadas fora do grep original por formatação em ternário/ficarem sem `'white'` literal: `TrailStatsTable.tsx`, `TrailCompletionModal.tsx`, `Certificate.tsx`, `TimesClient.tsx` — todas corrigidas.
  - **Achado maior, via 5.1 (rodar a varredura de verdade, não só grep):** o mesmo defeito na polaridade OPOSTA — `background: 'var(--ffv-blue)'` (ou gradiente) com `color: '#0d1117'` FIXO — não aparecia no grep de `'white'`/`'#fff'`, mas quebra do mesmo jeito: `--ffv-blue` é claro no tema ESCURO (texto escuro serve) e vira escuro no tema CLARO (texto escuro passa a falhar). A varredura (axe, rodada nesta tarefa) pegou isso ao vivo: **101 de 121 rotas `/aprenda/lab-*` e `/aprenda/arq-ia-aws-*` reprovavam** por um único botão `FalhaAoCarregar.tsx` ("Tentar novamente", componente novo do pack `estados-de-ui-resilientes` desta mesma sessão, 3,02:1 no tema claro). Sweep dedicado achou mais **21 ocorrências do mesmo padrão** em arquivos que eu já tinha tocado e em vários que não (`LoginModal.tsx`, `ReviewClient.tsx`, `TimesClient.tsx`, `QuizBlock.tsx`, `SimuladoRunner.tsx`, `ResultadoClient.tsx`, `CertificateModal.tsx`, `OnboardingModal.tsx`, `not-found.tsx`, `admin/layout.tsx`, `VerificarClient.tsx`, `PreferenciasClient.tsx`, `MapaClient.tsx`, `ComunidadeAutor.tsx`, `cheatsheet/page.tsx`, `claude-code-vs-cursor/page.tsx`, `melhores-ferramentas-ia-codigo-2026/page.tsx`) — todas corrigidas para `var(--primary-foreground)`. 3 ficaram de fora corretamente: `var(--ffv-gold)` é constante por tema (não flipa), texto escuro fixo já é o par certo ali (`RankingClient.tsx:593`, `HomeRanking.tsx:89,364`). Duas instâncias em `admin/layout.tsx`/`LoginModal.tsx` usavam gradiente de dois tons com um hex fixo (`#60a5fa`) como segundo stop, que não tem solução tema-correta com texto único — trocado o segundo stop para `var(--ffv-purple)`, igualando ao gradiente primário já comprovado seguro (mesmo padrão do `FfvButton`).
- [x] 1.2 Adicionar o par branco-sobre-acento ao `paleta-contraste.test.ts` — novo describe `branco sobre fundo acentuado`, mais `readable-text.test.ts` para o caso de cor de dado

## 2. Botão e cor únicos

- [ ] 2.1 Eleger `FfvButton` como botão único; migrar as chamadas inline (43 `var(--ffv-blue)` + gradientes) — NÃO feito como migração de MARKUP; feito como unificação de COR (ver 1.1/2.3: os call sites inline agora usam os mesmos tokens que `FfvButton`). Bloqueio real: `FfvButton`'s variante `primary` é um GRADIENTE azul→roxo, e a maioria dos ~35 call sites inline usa `var(--ffv-blue)` CHAPADO — trocar o markup mudaria a aparência de chapado pra gradiente em dezenas de botões, o que esbarra em "não redesenhar identidade visual". Migrar de verdade exigiria primeiro decidir (fora do escopo desta change) se `FfvButton` ganha uma variante chapada, ou se os call sites chapados são o padrão errado a corrigir. Documentado como decisão em aberto, não como trabalho perdido.
- [x] 2.2 Apagar `ui/button.tsx`, `ui/card.tsx`, `ui/badge.tsx`, `ui/separator.tsx` (0 imports) e os cards de módulo órfãos — eram 4, não 6: `NextModuleCard.tsx`, `TrailMap.tsx`, `RelatedModules.tsx`, `RelatedArticles.tsx`, todos com 0 imports fora de si mesmos, verificado antes de apagar. Achado à parte (fora de escopo, reportado): ~18 outros componentes de `src/components/article/` (TOC, bookmark, rating, prerequisites, code playground, text-selection-share etc.) também estão com 0 imports — `ModuleLayout.tsx`, que o `CLAUDE.md` documenta como o wrapper que os usava, não existe mais no HEAD; `/aprenda/[slug]/page.tsx` os desconectou antes desta sessão. Não apagados nem reconectados aqui — fora do escopo desta change, meritório de auditoria própria.
- [ ] 2.3 Trocar os 76 hex de tema à mão por tokens — PARCIAL. Redefinido com precisão: "hex que DUPLICA um valor de token existente" media **126 em 54 arquivos** no começo da sessão (não 76 — a régua da proposta provavelmente contava só um subconjunto). Caiu para **89** ao longo da sessão: parte pelas correções diretas de 1.x/3.x (`#f78166`→`var(--ffv-red)` etc.), e a maior fatia como efeito colateral do achado grande de 1.1 (as ~22 correções de `color:'#0d1117'`→`var(--primary-foreground)` também contavam como hex-de-token). As ~89 restantes, em arquivos não tocados nesta sessão, não foram migradas — volume grande demais. Protegido contra piora pelo gate 2.4 (teto=89, só desce)
- [x] 2.4 Gate de lint: hex literal em `style={{}}` reprova (exceto `opengraph-image`) — `scripts/check-hex-in-style.mjs`, ratchet (teto=89, só desce — caiu de 126 durante a sessão), wired em `npm run lint`/`lint:ci`. Verificado com prova negativa (arquivo temp com hex novo → falha; removido → volta a passar)
- [x] 2.5 Decidir espaçamento (classes OU `--space-*`) e remover o sistema morto — Tailwind classes venceu (0 consumidores de `--space-*` contra milhares de usos de `p-*`/`gap-*`/etc.); bloco removido de `globals.css`

## 3. Acessibilidade

- [x] 3.1 `useFocusTrap(ref, isOpen)` aplicado aos 13 modais (não 11 — recontado em 11/ago), contenção + restauração — `frontend/src/hooks/useFocusTrap.ts`, testado em `useFocusTrap.test.tsx`
- [x] 3.2 `tabIndex={0}`+`role=group`+`aria-label` nos contêineres de scroll — recontado em 11/ago: 15 fora de `primitives.tsx` (não 12) + 4 dentro de `primitives.tsx`/`BlockRenderer.tsx`/`AwsDiagram.tsx` (2 já corrigidos em ago/2026, 2 novos — `ArchDiagram`, `MatrixDiagram`). `admin/layout.tsx` recebeu só `tabIndex` (contêiner de layout, não "uma tabela" — `role=group` seria semanticamente errado ali)
- [x] 3.3 Remover os 3 `outline:'none'` inline — `GlossaryClient.tsx`, `LeadCaptureSection.tsx`, `FinalCta.tsx`; os 2 sem label visível ganharam `aria-label` no mesmo commit
- [x] 3.4 `aria-label`/`<label>` em todo input — `<textarea>` de comentários já tinha `htmlFor` (fix de `estados-de-ui-resilientes`); auditoria fresca achou 48 controles sem nome em 18 arquivos, todos corrigidos (label `htmlFor`/`useId()` nos componentes `Field`/`Inp`/`Sel`/`Input`/`Textarea`/`Select` reusados nos editores admin, `aria-label` nos filtros/sliders/campos avulsos). `Field` de `curriculum/edit` virou `<fieldset>`+`<legend>` (recebe o controle pronto via `children`, `htmlFor` não dava pra amarrar sem tocar 6 call sites)
- [x] 3.5 Rebaixar títulos do rodapé de `h3` para rótulo de agrupamento; corrigir ordem de heading — `FooterColumn` (`SiteFooter.tsx`) de `h3`→`p`; era 1 declaração/4 instâncias em runtime (um `<FooterColumn>` por coluna), não 4 declarações
- [x] 3.6 `min-height:44px` nos controles listados; `overflow-x-auto`+`tabIndex` na `TrailStatsTable` — `TrailStatsTable.tsx` (`overflow-hidden`→`overflow-x-auto`+`tabIndex`+`role=group`) e as ações de certificado em `Certificate.tsx`. Achado durante o fix: `Certificate.tsx` tinha o MESMO defeito de `trail.color`+texto escuro fixo do item 1 (`#0d1117` sobre `var(--ffv-blue)` falha 3,02:1 no tema claro) — corrigido com `readableTextColor`/`var(--primary-foreground)` no mesmo commit. O alternador estudo/prova citado na proposta foi confirmado REMOVIDO (pack `orcamento-de-carga-por-rota` desta sessão) — nada a fazer

## 4. Navegação

- [x] 4.1 `Breadcrumb` único (nav>ol, com o item atual) aplicado onde falta — PARCIAL, ver nota. Componente novo `src/components/Breadcrumb.tsx` (`nav>ol`, `aria-current="page"` no item atual — nenhuma das 24 instâncias pré-existentes tinha isso). Aplicado em `/progresso` e `/plano` (as 2 das 6 rotas pessoais sem nenhuma forma de wayfinding). NÃO tocado: `/aprenda/[slug]` (breadcrumb já semântico, casa com `BreadcrumbList` do JSON-LD, risco desproporcional) nem as ~18 outras instâncias copiadas à mão em páginas não-pessoais (fora do escopo priorizado dado o tempo — proposta pedia "aplicado onde falta, **especialmente rotas pessoais**", que é o que foi feito)
- [x] 4.2 Botão "voltar" único; presente nas rotas pessoais — `src/components/BackButton.tsx` (ícone `ArrowLeft` do lucide-react, não mais o glifo `←` cru). Migrados 11 call sites: as 4 rotas pessoais restantes que já tinham back-link próprio (`/perfil`, `/devcard`, `/revisar`, `/times` — via `DevProfileClient`, `DevCardClient`, `ReviewClient`, `TimesClient`) + 7 fora do pessoal (`SimuladoDetailClient`, `maratona`, `TrailBlogClient`, `SimuladoRunner`, `ResultadoClient`×2, `privacidade`). NÃO migrados: 2 back-links com estilo de botão CTA full-width (`ResultadoClient.tsx:260`, `PreferenciasAprendizadoClient.tsx:269`) — converter mudaria o peso visual, fora do "não redesenhar"
- [x] 4.3 Busca na barra inferior do mobile, alvo 44px — PARCIAL. `CommandPaletteTrigger` (o botão de busca sempre visível no HUD, ícone-só no mobile) tinha `height: 32` fixo; virou `h-11` (44px) no mobile, `sm:h-8` no desktop. NÃO feito: adicionar um 6º ícone dedicado de busca na barra de 5 slots do `MobileNav` — mudança estrutural maior, a proposta já sinalizava isso como possivelmente menor prioridade ("use judgment on effort vs. gain")

## 5. Travar

- [x] 5.1 Rodar `npm run varredura` (axe, 535+ telas) e conferir a queda de violações — rodado como `npx playwright test -c playwright.varredura.config.ts -g "acessibilidade"` (a checagem de axe especificamente; a varredura completa tem 15 checagens não relacionadas — SEO, bundle etc. — fora do escopo desta change) contra build de produção real (`npm run build`). **Antes da correção do achado em 1.1: 101 de 121 rotas reprovando** (contraste). **Depois: 121 de 121 passando, 0 violações.** Não rodei a varredura completa (535+ telas, até 40min) por orçamento de tempo da sessão — rodei a checagem de acessibilidade especificamente, que é o que esta tarefa pede
- [x] 5.2 Atualizar `frontend/CLAUDE.md` com o botão único e o gate de hex — ver seção Acessibilidade
